name: Deploy Updated Stack

on:
  push:
    branches:
      - main  # Triggered on push to main (e.g. Renovate merge)

jobs:
  deploy:
    name: Update and Restart Docker Compose Stack
    runs-on: [self-hosted, homelab]

    steps:
      - name: Pull latest images
        run: docker compose -p docker-stack pull
        working-directory: /home/esteban/docker-stack

      - name: Restart containers
        run: docker compose -p docker-stack up -d --no-build --remove-orphans
        working-directory: /home/esteban/docker-stack

