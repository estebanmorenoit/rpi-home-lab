name: Local Deploy on RPi

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run local deploy script
        run: |
          echo "Starting local deployment..."
          /home/esteban/docker-stack/deploy.sh
